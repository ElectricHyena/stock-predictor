# =====================================================================
# PORT CONFIGURATION - Environment Variables
# =====================================================================
# All services use port mappings defined via environment variables.
# This enables flexible port configuration and prevents conflicts.
#
# If ports are already in use on your system, change them here before
# starting Docker Compose. Services communicate internally via service
# names (postgres:5432, redis:6379), not localhost.
#
# Example: If port 5432 is in use locally:
#   POSTGRES_PORT=5433
#   Then: docker-compose up -d
# =====================================================================

# ===== DATABASE PORT =====
# PostgreSQL port mapping (host:container)
# Using 7000 series to avoid conflicts with other projects
POSTGRES_PORT=7001

# ===== REDIS PORT =====
# Redis port mapping (host:container)
# Using 7000 series to avoid conflicts with other projects
REDIS_PORT=7002

# ===== API PORT =====
# FastAPI application port mapping (host:container)
# Using 7000 series to avoid conflicts with other projects
API_PORT=7003

# =====================================================================
# DATABASE CONFIGURATION
# =====================================================================
# PostgreSQL connection credentials
POSTGRES_USER=dev
POSTGRES_PASSWORD=devpass
POSTGRES_DB=stock_predictor

# =====================================================================
# FASTAPI CONFIGURATION
# =====================================================================
DEBUG=True
HOST=0.0.0.0
PORT=8000

# =====================================================================
# REDIS & CELERY CONFIGURATION
# =====================================================================
# Note: Celery URLs are constructed internally using REDIS_PORT
# No need to change CELERY_* variables unless customizing further
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# =====================================================================
# API KEYS & SECURITY
# =====================================================================
# News API Key for fetching financial news
NEWSAPI_KEY=your_newsapi_key_here

# Secret key for JWT token generation (change in production!)
SECRET_KEY=dev-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =====================================================================
# CORS & FRONTEND
# =====================================================================
# Allowed origins for CORS (comma-separated)
# Update with your frontend URL in production
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://127.0.0.1:3000

# =====================================================================
# LOGGING
# =====================================================================
# Log level: debug, info, warning, error, critical
LOG_LEVEL=info
